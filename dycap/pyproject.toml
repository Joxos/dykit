[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dycap"
version = "0.2.0"
description = "Douyu Live Stream Danmu Collector - async library and CLI for collecting chat messages into per-run SQLite files."
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
    {name = "Douyu Danmu Crawler Project", email = "contact@example.com"}
]
keywords = ["douyu", "danmu", "chat", "websocket", "collector", "sqlite"]
dependencies = [
    "dycommon>=0.1.0",
    "dysource>=0.1.0",
    "dyproto>=0.1.0",
    "websockets>=12.0",
    "cyclopts",
    "rich>=14.0.0",
    "loguru>=0.7.0",
    "tenacity>=9.1.2",
]

[project.scripts]
dycap = "dycap.cli:main"

[tool.uv.sources]
dycommon = { workspace = true }
dysource = { workspace = true }
dyproto = { workspace = true }

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.basedpyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
